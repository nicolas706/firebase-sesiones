deployment:
  tasks:
    - export DEPLOY_PATH="/home/corencl/public_html/clientes.coren.cl/wp-content/plugins/firebase-sesiones"
    - /bin/mkdir -p "$DEPLOY_PATH"
    - /bin/rsync -a --delete --exclude='.git' --exclude='.github' --exclude='.cpanel.yml' ./ "$DEPLOY_PATH"/